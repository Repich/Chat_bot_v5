5.0.0-alpha.82
- Добавлена вкладка "Обучение" с компактной сводкой auto-learned навыков: создано, активно, переиспользовалось, с ошибками и автоблокировано.
- На вкладке "Обучение" показываются карточки learned skills с runtime health, последними вопросами переиспользования и переходом в мастерскую навыков.
- Static asset URLs обновлены до alpha.82.

5.0.0-alpha.81
- Static asset URLs обновлены до alpha.81 для cache-busting после backend-исправления generic entity lookup.

5.0.0-alpha.80
- Static asset URLs обновлены до alpha.80 для cache-busting после backend-изменений в пайплайне навыков.

5.0.0-alpha.79
- Мастерская навыков упрощена до каталога навыков: вкладки кандидатов, черновиков, утверждения, promotion и regression replay убраны из основного UI.
- Карточка learned/user-created навыка показывает редактируемый запрос 1С и implementation JSON; доступны только действия "Сохранить изменения" и "Удалить".
- Базовые системные навыки отображаются read-only с понятным сообщением, что они защищены от правки в интерфейсе.
- Клиент больше не привязывает обработчики к удаленным lifecycle-кнопкам и не ведет пользователя в старый workflow черновиков/кандидатов.

5.0.0-alpha.78
- В карточке навыка появилось локальное поле "Комментарий проверки"; действия "Отметить проверенным" и "Заблокировать" берут причину из этой карточки, а не только из общего блока жизненного цикла.
- Static assets обновлены до alpha.78 для cache-busting после исправления lifecycle-кнопок.

5.0.0-alpha.77
- В карточке "Обобщенный кандидат от агента" появилась кнопка "Отклонить"; действие убирает learned-кандидат из активной очереди без потери audit history.
- Если learned-кандидат не хранит готовый текст запроса, карточка показывает блок "Шаблон запроса" с типом спецификации, источником 1С, полем периода, метриками или привязками параметров.
- Для learned-кандидатов источники 1С теперь берутся из metadata dependencies, если trace summary недоступен.

5.0.0-alpha.76
- Вкладка "Кандидаты агента" теперь показывает обобщенные learned skill candidates как отдельный тип карточки.
- Для learned skill candidate основное действие называется "Открыть навык" и ведет в карточку навыка; кнопки создания черновика raw synthesis для него не показываются.
- Подсказка вкладки уточняет различие между raw synthesis candidates и обобщенными learned skill candidates.

5.0.0-alpha.75
- Поведение web-клиента не менялось. Static assets подключаются с новой версией в URL для cache-busting после backend-обновления.

5.0.0-alpha.74
- Поведение web-клиента не менялось. Static assets подключаются с новой версией в URL для cache-busting после backend-обновления.

5.0.0-alpha.73
- Поведение web-клиента не менялось. Static assets подключаются с новой версией в URL для cache-busting после backend-обновления.

5.0.0-alpha.72
- Поведение web-клиента не менялось. Static assets подключаются с новой версией в URL для cache-busting после backend-обновления.

5.0.0-alpha.71
- Карточка кандидата агента теперь показывает данные, достаточные для решения: цель, интерпретацию агентом, критерии отбора, параметры, использованные источники 1С, запрос, строки MCP, проверки и причины достаточности результата.
- Все найденные metadata objects перенесены в технический блок, чтобы не смешивать реально использованные источники с шумом поиска.
- В карточку добавлен блок "Что проверить перед решением" с предупреждениями о семантических рисках и дублирующихся строках результата.

5.0.0-alpha.70
- Поведение web-клиента не менялось. Static assets подключаются с новой версией в URL для cache-busting после backend-обновления.

5.0.0-alpha.69
- Поведение web-клиента не менялось. Static assets подключаются с новой версией в URL для cache-busting после backend-обновления.

5.0.0-alpha.68
- Поведение web-клиента не менялось. Static assets подключаются с новой версией в URL для cache-busting после backend-обновления.

5.0.0-alpha.67
- Поведение web-клиента не менялось. Static assets подключаются с новой версией в URL, чтобы браузер забрал актуальные файлы после backend-обновления.

5.0.0-alpha.66
- Результат или ошибка действия черновика теперь показывается прямо под action-кнопками карточки, чтобы нажатие "Опубликовать как кандидат" не выглядело как отсутствие реакции.
- Блокеры публикации черновика показываются русскими сообщениями рядом с кнопками.
- Static assets подключаются с версией в URL, чтобы браузер после обновления страницы подтягивал актуальные JS/CSS без старого cache.

5.0.0-alpha.65
- Action-кнопки Workbench теперь обрабатываются единым document-level обработчиком внутри раздела "Мастерская навыков", а не только внутри текущего блока результата.
- Ошибки draft actions, запущенных не из карточки, теперь показываются в основном блоке результата вместо незаметного глобального состояния.

5.0.0-alpha.64
- Результаты действий черновика теперь выводятся внутри той же карточки рядом с кнопками, чтобы пользователь видел отклик без прокрутки к общему блоку результата.
- Ошибки approve/publish вроде отсутствующего smoke или approval остаются inline в карточке и не выглядят как отсутствие реакции интерфейса.
- Во время выполнения действия карточка показывает локальный прогресс.

5.0.0-alpha.63
- Действия черновика "Предпросмотр", "Проверить", "Утвердить проверку" и "Опубликовать как кандидат" больше не заменяют карточку черновика отдельным экраном результата.
- После действия Workbench показывает блок результата над обновленной карточкой черновика, поэтому поля комментария и кнопки жизненного цикла остаются доступны.
- Результат предпросмотра теперь показывает запрос 1С, лимит, параметры и замечания проверки.
- Результат утверждения получил человекочитаемый рендер вместо незаметного технического ответа.

5.0.0-alpha.62
- В карточки черновиков добавлены локальные поля "Комментарий проверки" и "Тестовые параметры"; кнопки жизненного цикла берут значения из своей карточки.
- Проверка обязательного комментария выполняется до запуска Workbench action, поэтому экран с черновиком и кнопками не заменяется технической ошибкой.
- Ошибка пустого комментария показывается внутри карточки черновика, фокус переводится в поле комментария.

5.0.0-alpha.61
- Карточка черновика в Workbench теперь показывает бизнес-смысл, исходный вопрос и ответ агента, источники 1С, роли полей, расчет, запрос 1С, параметры и заметки агента.
- Действия черновика переименованы в "Утвердить проверку" и "Опубликовать как кандидат"; в карточке добавлено объяснение разницы между этими шагами.
- Добавлена кнопка "Удалить" для черновика с подтверждением и обновлением списка после удаления.

5.0.0-alpha.60
- После создания черновика из кандидата агента Workbench показывает понятную подсказку со следующим шагом, а не только техническую карточку результата.
- Карточка кандидата агента с уже связанным черновиком показывает действие "Открыть черновик" и сам идентификатор черновика.
- Добавлена frontend-регрессия на отображение связанного черновика и подсказки после действия.

5.0.0-alpha.59
- Исправлено отклонение карточек "Кандидаты агента": action-кнопки теперь явно отменяют стандартное поведение клика и не могут привести к перезагрузке страницы.
- После отклонения или игнорирования похожих кандидатов Workbench перезагружает активную очередь и показывает понятное сообщение о выполненном действии.
- Вкладка "Кандидаты агента" по умолчанию показывает только активные candidate-карточки, чтобы отклоненные кандидаты не выглядели как не обработанные.

5.0.0-alpha.58
- Карточка навыка в Workbench теперь показывает человекочитаемый контракт: что делает навык, когда выбирается, какие входы принимает, какой результат возвращает, какие фильтры/признаки поддерживает и какой runtime использует.
- Для известных стратегий выполнения добавлены русские объяснения, чтобы консультант видел бизнес-смысл навыка, а не только идентификатор и путь к JSON.

5.0.0-alpha.57
- Уточнены подписи действий жизненного цикла навыков: вместо технического "Повысить статус" используются "Отметить проверенным" и "Применить статус".
- Внутреннее API-действие promote не изменялось, чтобы сохранить совместимость Workbench backend.

5.0.0-alpha.56
- Убрана отдельная правая панель Workbench с дублирующей кнопкой "Утвердить".
- Поля "Комментарий утверждения" и "Тестовые параметры" перенесены в основную область Workbench; кнопки на карточках черновиков используют эти значения.
- Дополнительные действия по идентификаторам, жизненному циклу и регрессии оставлены в свернутых блоках основной области.

5.0.0-alpha.55
- Исправлено отображение ошибок Workbench: вместо JavaScript stack trace показывается понятная причина на русском языке.
- Добавлены человекочитаемые сообщения для backend-кодов approval_comment_required, current_successful_smoke_required и ошибок жизненного цикла навыков.
- Утверждение и публикация черновика больше не зависят от smoke_id, сохраненного только в памяти страницы: если он не передан, backend использует последний актуальный успешный проверочный запуск.

5.0.0-alpha.54
- Переведены видимые подписи web-клиента, карточки мастерской навыков, сообщения действий, статусы и административные подсказки на русский язык.
- Смешанные англо-русские подписи действий, разделов и карточек заменены на русские формулировки.
- Внутренние имена действий API и технические ключи JSON оставлены без изменений, чтобы сохранить совместимость endpoint и уже сохраненных артефактов.

5.0.0-alpha.53
- Added an explicit "Открыть trace" action to Workbench candidate cards when trace_path is available.
- Workbench now opens the selected candidate trace path in the summary/details area without requiring manual JSON inspection.
- Extended the JavaScriptCore renderer regression check to cover the trace action.

5.0.0-alpha.52
- Fixed Workbench renderer priority so responses containing both summary and candidates/skills/drafts/objects render the entity list instead of only total/by_status.
- Reworked Workbench result cards for skills, drafts, metadata objects, and candidates with human-readable titles, statuses, facts, tags, and primary actions.
- Added card-level actions for agent/onboarding candidates, drafts, and skills so users can create drafts or run next checks without copying IDs into side-panel fields.
- Moved manual ID actions and the raw draft builder into advanced sections, leaving the main Workbench flow centered on queues and cards.
- Added JavaScriptCore regression coverage for agent candidates with summary plus candidate rows.

5.0.0-alpha.51
- No frontend behavior changes. This release fixes backend sufficiency review for valid counterparty debt rows.

5.0.0-alpha.50
- Split the web client out of the Python HTTP router into static index.html, styles.css, api.js, renderers.js, workbench.js, and app.js.
- Reworked the client into top-level Chat, Workbench, Metadata, Documentation, and Administration views instead of hiding Workbench controls inside chat settings.
- Added admin token storage and token-aware fetchAdmin/adminHeaders wrappers for all Workbench admin API calls.
- Added safe DOM helpers, global UI error reporting, shared button busy/error states, and Workbench result cards with raw JSON hidden in technical details.

5.0.0-alpha.49
- Added a safe in-memory storage fallback so Safari private browsing does not stop the page script before button handlers are registered.
- Collapsed advanced Workbench draft, approval, candidate, lifecycle, regression, and debug controls into explicit subpanels so settings stay readable by default.

5.0.0-alpha.48
- No frontend behavior changes. This release fixes server startup so the alpha.47 Documentation UI is actually loaded after restart.

5.0.0-alpha.47
- Added a Documentation section to settings with the full docs index, in-client document viewer, and quick Workbench skill workflow actions.
- Fixed the Skill Catalog button regression caused by a stale action variable and restored automatic smoke_id capture after Smoke.

5.0.0-alpha.46
- Workbench metadata object cards continue to show confirmed fields and now use all_fields for field picker actions so hints remain visible but separated.
- Added Smoke params JSON input to the Workbench panel for test values required by parameterized draft smoke runs.
- Draft approval and candidate publication actions now send the latest successful smoke_id; publication sends explicit approve=true and approval comment.
- No UI behavior changes from the module split; the web client source now lives outside the backend HTTP router.

5.0.0-alpha.45
- No frontend behavior changes. Release cleanup updated repository ignore rules for Workbench runtime evidence.

5.0.0-alpha.44
- Workbench action responses may now include trace_path so the web UI/raw JSON can point support diagnostics to compact Workbench trace folders.

5.0.0-alpha.43
- Metadata object cards now include actions to use the object as a draft source and to place fields into group, metric, or filter slots of the guided draft builder.
- Field selection remains generic and does not hard-code object-specific classifiers; consultants choose the business role through the builder.

5.0.0-alpha.42
- No frontend UI changes. Workbench preview results may now show metadata-review errors based on actual metadata lookup instead of draft-only confirmation.

5.0.0-alpha.41
- Updated Workbench user documentation for the summary panel, skill/draft/object cards, lifecycle controls, regression replay, and guided top_n_by_metric draft creation.

5.0.0-alpha.40
- Added a guided top_n_by_metric draft builder to the Workbench UI so a consultant can create a HumanSkillDraft without writing JSON.
- The builder captures source object, grouping field, metric field, optional filter field, aggregation, limit, and field confirmation state.
- Raw Draft JSON remains available as an advanced override, but the default manual creation path now produces structured draft payloads.

5.0.0-alpha.39
- Added a human-readable Workbench summary panel above raw JSON results.
- Skill catalogs, skill cards, drafts, candidates, metadata objects, lifecycle results, and regression replay now render concise business/technical cards.
- Added Workbench actions to open a specific skill, draft, or metadata object by id/full_name.

5.0.0-alpha.38
- Added Skill Workbench controls for skill lifecycle actions: promote, rollback, deprecate, and block.
- Added a web control to run regression replay from the Workbench settings panel.
- Lifecycle actions can now include a reason, regression case ids, target status, successful run count, and explicit admin approval flag.

5.0.0-alpha.32
- Added Workbench controls for agent-generated query synthesis candidates.
- The web client can load the synthesis candidate queue, create a draft from a selected candidate, reject a candidate, or ignore similar generated queries.

5.0.0-alpha.31
- Added Skill Workbench controls to load onboarding candidates, create a draft from a selected candidate, and reject a candidate.

5.0.0-alpha.29
- Added Workbench controls for approval comments plus explicit approve/reject actions.
- Workbench candidate publication now sends approval comments through the admin API.

5.0.0-alpha.13
- Fixed the training banner layout so a completed onboarding message no longer consumes the main viewport.
- Expanded the chat area to use the available screen width and height.
- Moved refresh, backend/frontend history, onboarding launch, and ProductRef controls into a collapsible settings panel.
- Added a visible sessions list that combines browser-saved session ids with sessions currently held by the server process.
- Added a new-session action in the sidebar.

5.0.0-alpha.11
- Added initial-training status banner to the web client.
- Added an admin block for configuration dump path, onboarding launch, and training counters.
- Added polling for background onboarding status so the page updates after training finishes.

5.0.0-alpha.6
- Added Enter-to-send in the chat input; Shift+Enter still inserts a new line.
- Added unread assistant/error notification by blinking the browser tab title while the tab/window is not active.
- The unread title indicator clears when the browser tab becomes visible or focused.

5.0.0-alpha.2
- Kept the message composer visible while long dialogs scroll.
- Added visible bot version in the header.
- Added browser-side session id persistence.
- Added automatic conversation history loading after browser reload.
- Added manual dialog refresh action.
- Added backend and frontend history viewers in the web client.
- Improved empty/error states for history loading.

5.0.0-alpha.1
- Added first embedded web chat client for local testing.
