# Raw Sources

This directory is reserved for source metadata and raw-capture manifests.

Current rule: do not place protected official documentation or book text in the repository.

Protected full-text captures are stored outside Nextcloud under:

`/Users/olegrepnikov/Library/Application Support/WorkAssistant4/runtime/wiki/1c-language/raw/`

Use `manifest.md` and `sources/manifest.md` as the source catalog. Record local paths, checksums, byte sizes, and capture timestamps.
